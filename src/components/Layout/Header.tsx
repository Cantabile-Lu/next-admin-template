import { FC } from "react";

interface Props {}

const Header: FC<Props> = (props) => {
    return <header className={"h-[0.6rem] w-[4.02rem] bg-primary-color"}>hello react</header>;
};

export default Header;
